# privateVault (Week 1 Foundation)

- Use `electron-forge` scripts (`npm run start`, `npm run make`) for local development and Forge packaging flows.
- Use `electron-builder` scripts (`npm run build:win`, `npm run build:mac`, `npm run build:both`) for release artifacts.
